import { Box } from '@chakra-ui/react'
import styled from '@emotion/styled'

export const BioSection = styled(Box)`
  margin-top: 3rem;
  display: block;
  font-size: 16px;
`

export const BioYear = styled.span`
  font-weight: bold;
  margin-right: 1em;
`

export const BioLi = styled.div`
  margin-top: 1rem;
  text-align: left;
  margin-left: 1rem;
  line-height: 2rem;
  overflow: hidden;
  white-space: pre-wrap;
`
